version: "3.9"

services:
  portainer:
    container_name: portainer
    image: portainer/portainer-ce:lts
    restart: unless-stopped
    labels:
      - "wud.watch=false"
      - "wud.watch.digest=true"
    ports:
      - "${PRIVATE_BIND_IP:-127.0.0.1}:${PORTAINER_HTTPS_PORT}:9443"
      - "${PRIVATE_BIND_IP:-127.0.0.1}:${PORTAINER_EDGE_PORT}:8000"
    volumes:
      - ${DOCKER_SOCK_PATH}:/var/run/docker.sock
      - ${PORTAINER_DATA_PATH}:/data
    environment:
      TZ: ${TZ}
    networks:
      - tools

networks:
  tools:
    name: tools
    driver: bridge
